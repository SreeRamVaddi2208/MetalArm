[pytest]
testpaths = tests
# Surface warnings (e.g. a future SQLAlchemy/pydantic deprecation) instead of
# letting them accumulate silently until an upgrade breaks the build.
filterwarnings =
    default
addopts = -q --strict-markers
